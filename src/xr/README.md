# XR module

This directory owns XR session and physical-input adapters. It may emit only the discrete commands defined by `src/app/commands.ts` or development-only preflight facts consumed by `src/app`; it must not import simulation internals. The local Quest 3 route is proven, while hosted, ergonomic, endurance, and release gates remain device-specific work.
