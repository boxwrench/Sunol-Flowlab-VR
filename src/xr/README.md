# XR module

This directory will own XR session and physical-input adapters. It may emit only the discrete commands defined by `src/app/commands.ts` and must not import simulation internals. Runtime XR work remains device-gated.

